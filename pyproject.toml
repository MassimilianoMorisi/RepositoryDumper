[project]
name = "repository-dumper"
version = "0.1.0"
description = "Export a repository into a single structured text snapshot"
requires-python = ">=3.10"

[project.scripts]
repo-dump = "repository_dumper.cli:main"